# S1GaLib

This library relies on the [S1Core](https://github.mit.edu/ALFAGroup/S1Core) library to function correctly. It expects the S1Core folder to be at the same directory level as itself. Adding both this library and S1Core as submodules to your main project is recommended.